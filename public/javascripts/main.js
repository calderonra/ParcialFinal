window.onload =()=>{
    app.int;
}

let app ={
    init: function () {
        this.addEvents;
        this.loadContent;
    },
    addEvents:function () {
        document.addEventListener("Submit",(event)=>{
            this.SubmitPost(event,this.addRow);
        });
    },
    addRow: function (data) {
        tbody=document.getElementsByClassName("Color")[0];
        tr=document.createElement('tr');
        tr.innerHTML=`<td`
    },

    deletePost: function () {
        
    },
    SubmitPost: (event,addRow)=>{
        event.preventDefault();
        let data ={
            
        }
    } 
        


}